Run the following:
```shell
git clone https://github.com/atharvaaalok/quick_cl.git

# Install uv
curl -LsSf https://astral.sh/uv/install.sh | sh
```

The do:
1. Go into repo.
1. In command line run `uv sync`.
1. Open `find_max_cl_in_database.ipynb`
1. Set kernel to `quick_cl`.
1. Run the notebook.